pipeline {
    agent any
    stages{
        stage("build"){
            steps{
                echo "In the build stage"
                sh 'pwd'
                sh 'ls -lht'
            }
        }
    }
}